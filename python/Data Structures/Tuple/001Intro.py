# Like a list, 
# but immutable — once created, it cannot be changed.

# Key traits:
# Ordered, Immutable, and Can contain duplicates.
# Often used for fixed data or returning multiple values from functions.

coordinates = (10,20)
print(coordinates[0])
print(len(coordinates))